export default function Home() {
  return (
    <div style={{fontFamily: 'Inter, system-ui, -apple-system, Roboto, sans-serif', textAlign: 'center', marginTop: 80 }}>
      <h1>Invoice Manager (MVP) — Running 🚀</h1>
      <p>This is a minimal starter app created for OAuth redirect testing and Vercel deployment.</p>
      <p>Visit <code>/api/auth/callback/google</code> to verify OAuth callback.</p>
    </div>
  )
}
